@model List<Ys.Model.Model.Recruit.ResumeModel>
@{
    ViewBag.Title = "resume";
    Layout = "";
}

<!DOCTYPE html>
<html>
<head>
    <title></title>
</head>
<body>
    <div>      
            @{          
                for (int i = 0; i < Model.Count; i++)
                {
                @Html.ActionLink(Model[i].Name,"Index","Resume",new { resumeId = Model[i].Id }, null)
                }
           }
        
    </div>
</body>
</html>
